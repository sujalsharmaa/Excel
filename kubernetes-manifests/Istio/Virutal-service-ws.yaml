apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: backend-ws-virtualservice
spec:
  hosts:
    - "*"
  gateways:
    - sheetwise-ws-gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: backend-ws.default.svc.cluster.local
            port:
              number: 8080
